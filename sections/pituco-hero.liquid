{% schema %}
{
  "name": "Pituco Hero Section",
  "settings": [
    {
      "type": "header",
      "content": "Hero Content"
    },
    {
      "type": "text",
      "id": "title_line1",
      "label": "Title Line 1",
      "default": "Stilinizi"
    },
    {
      "type": "text",
      "id": "title_line2",
      "label": "Title Line 2 (Pink)",
      "default": "Duvar Sanatına"
    },
    {
      "type": "text",
      "id": "title_line3",
      "label": "Title Line 3",
      "default": "Dönüştürün"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Fotoğraflarınızla, metinlerinizle ve tarzınızla kişiselleştirilmiş posterler yaratın. Premium kalitede baskılar, kapınıza kadar teslim."
    },
    {
      "type": "text",
      "id": "button1_text",
      "label": "Button 1 Text",
      "default": "Kendi Posterini Oluştur"
    },
    {
      "type": "url",
      "id": "button1_link",
      "label": "Button 1 Link"
    },
    {
      "type": "text",
      "id": "button2_text",
      "label": "Button 2 Text",
      "default": "Alışverişe Başla"
    },
    {
      "type": "url",
      "id": "button2_link",
      "label": "Button 2 Link",
      "default": "/collections/all"
    },
    {
      "type": "text",
      "id": "feature1",
      "label": "Feature 1",
      "default": "✨ Sizin hikayeniz için özel"
    },
    {
      "type": "text",
      "id": "feature2",
      "label": "Feature 2",
      "default": "💕 Aşkınız için özel"
    },
    {
      "type": "text",
      "id": "feature3",
      "label": "Feature 3",
      "default": "🎨 Tarzınız için özel"
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#0a0a0a"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "Accent Color (Pink)",
      "default": "#D84575"
    }
  ],
  "presets": [
    {
      "name": "Pituco Hero"
    }
  ]
}
{% endschema %}

<section class="pituco-hero" style="background-color: {{ section.settings.bg_color }}; color: {{ section.settings.text_color }};">
  <div class="pituco-hero-container">
    <div class="pituco-hero-content">
      <h1 class="pituco-hero-title">
        <span class="title-line1">{{ section.settings.title_line1 }}</span>
        <span class="title-line2" style="color: {{ section.settings.accent_color }};">{{ section.settings.title_line2 }}</span>
        <span class="title-line3">{{ section.settings.title_line3 }}</span>
      </h1>
      
      <p class="pituco-hero-description">{{ section.settings.description }}</p>
      
      <div class="pituco-hero-buttons">
        {% if section.settings.button1_text != blank %}
          <a href="{{ section.settings.button1_link }}" class="pituco-btn pituco-btn-outline">
            {{ section.settings.button1_text }}
          </a>
        {% endif %}
        {% if section.settings.button2_text != blank %}
          <a href="{{ section.settings.button2_link }}" class="pituco-btn pituco-btn-primary" style="background-color: {{ section.settings.accent_color }};">
            {{ section.settings.button2_text }}
          </a>
        {% endif %}
      </div>
      
      <div class="pituco-hero-features">
        {% if section.settings.feature1 != blank %}
          <span class="feature-item">{{ section.settings.feature1 }}</span>
        {% endif %}
        {% if section.settings.feature2 != blank %}
          <span class="feature-item">{{ section.settings.feature2 }}</span>
        {% endif %}
        {% if section.settings.feature3 != blank %}
          <span class="feature-item">{{ section.settings.feature3 }}</span>
        {% endif %}
      </div>
    </div>
  </div>
</section>

<style>
  .pituco-hero {
    width: 100%;
    min-height: 600px;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 5rem 1rem; /* py-20 / px-4 benzeri */
    position: relative;
  }

  .pituco-hero-container {
    max-width: 896px; /* max-w-4xl */
    width: 100%;
    margin: 0 auto;
  }

  .pituco-hero-content {
    text-align: center;
    margin: 0 auto;
  }

  /* --- TITLE AYARLARI (Referans Koda Göre) --- */
  .pituco-hero-title {
    /* Font-serif */
    font-family: serif; 
    /* Font-bold */
    font-weight: 700;
    /* Tracking-tight (-0.025em) */
    letter-spacing: -0.025em;
    line-height: 1.1;
    margin-bottom: 2rem;
    
    /* React kodundaki gibi kelimelerin akmasını sağlamak için flex column yerine block kullanıyoruz */
    display: block; 
  }

  /* Text-5xl (Mobile Base) */
  .pituco-hero-title {
    font-size: 3rem; /* 48px */
  }

  /* Text-7xl (Tablet/MD Breakpoint) */
  @media screen and (min-width: 768px) {
    .pituco-hero-title {
      font-size: 4.5rem; /* 72px */
    }
  }

  /* Text-8xl (Desktop/LG Breakpoint) */
  @media screen and (min-width: 1024px) {
    .pituco-hero-title {
      font-size: 6rem; /* 96px */
    }
  }

  .title-line1,
  .title-line2,
  .title-line3 {
    /* React tasarımında kelimeler yan yanaydı, burada da inline-block ile akışkan bırakıyoruz */
    display: inline-block;
    margin: 0 6px; /* Kelimeler arası hafif boşluk */
  }

  .title-line2 {
    font-style: italic;
    /* Parent zaten serif olduğu için buraya tekrar yazmaya gerek yok */
  }

  /* --- DESCRIPTION AYARLARI --- */
  .pituco-hero-description {
    /* Text-xl (Mobile) */
    font-size: 1.25rem; /* 20px */
    line-height: 1.75rem;
    margin-bottom: 2rem;
    /* Text-muted-foreground (Hafif opaklık) */
    color: rgba(255, 255, 255, 0.7);
    max-width: 672px; /* max-w-2xl */
    margin-left: auto;
    margin-right: auto;
  }

  /* Text-2xl (Tablet/MD) */
  @media screen and (min-width: 768px) {
    .pituco-hero-description {
      font-size: 1.5rem; /* 24px */
      line-height: 2rem;
    }
  }

  /* --- BUTTON AYARLARI --- */
  .pituco-hero-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 2rem;
    padding-top: 1rem;
  }

  .pituco-btn {
    /* h-14 (56px) olmasını sağlamak için padding ayarı */
    padding: 16px 32px; /* px-8 */
    border-radius: 8px; /* Default radius */
    
    /* Text-lg */
    font-size: 1.125rem; /* 18px */
    font-weight: 600; /* Genellikle butonlar medium/semibold olur */
    
    text-decoration: none;
    transition: all 0.3s ease;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    min-height: 56px; /* h-14 garantisi */
  }

  .pituco-btn-outline {
    border: 1px solid rgba(255,255,255,0.2); /* React kodunda bg-bright var, burada outline taklit ediyoruz */
    background-color: #fff;
    color: #000; /* Bright theme usually means light background dark text */
  }

  .pituco-btn-outline:hover {
    background-color: rgba(255, 255, 255, 0.9);
  }

  .pituco-btn-primary {
    border: none;
    color: #fff;
  }

  .pituco-btn-primary:hover {
    filter: brightness(1.1);
  }

  /* --- FEATURES (ALT YAZI) AYARLARI --- */
  .pituco-hero-features {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
    /* Text-sm */
    font-size: 0.875rem; /* 14px */
    /* Text-muted-foreground */
    color: rgba(255, 255, 255, 0.7);
    padding-top: 2rem;
  }

  .feature-item {
    display: flex;
    align-items: center;
  }

  /* Araya nokta koymak istersen CSS ile ekleyebiliriz, şimdilik sadece boşluk var */
  .feature-item:not(:last-child)::after {
    content: "•";
    margin-left: 1rem;
    opacity: 0.5;
  }

  @media screen and (max-width: 768px) {
    .pituco-hero {
      padding: 5rem 1rem;
    }

    .pituco-hero-buttons {
      flex-direction: column;
      align-items: stretch;
    }

    .pituco-btn {
      width: 100%;
    }

    .pituco-hero-features {
      flex-direction: column;
      gap: 0.5rem;
      align-items: center;
    }
    
    .feature-item:not(:last-child)::after {
      content: ""; /* Mobilde noktayı kaldır */
      margin: 0;
    }
  }
</style>